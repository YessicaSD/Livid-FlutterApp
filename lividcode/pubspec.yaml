name: lividcode
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.0.1
  cloud_firestore: ^0.13.0+1
  #image_picker: ^0.6.1
  firebase_storage: ^3.0.10
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - lib/info/tasksExamples.json
    - lib/assets/
    